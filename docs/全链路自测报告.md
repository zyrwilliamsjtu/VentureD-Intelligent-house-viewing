# Agent 全链路自测报告

> **性质**：backend 内 TestClient / 真实 API 自测。  
> **产出**：`backend/tests/acceptance/test_agent_full_link.py`、本报告。  
> **日期**：2026-08-28  
> **分支**：`dev-backend`（相对 `e9884c4` 另有 fix 提交）  
> **世界**：`w_0330_840483`（0330）  
> **对照**：SPEC v2.2、`docs/AGENT_DEV.md`、`mock/real_0330/`

**PI 批准修复（同日）**：🔴 ASR 路由改为同步 `def asr`（线程池 + `asyncio.run`）；🟡 导航 intent 不走 LLM 改写。复测见 §4–§5。

---

## 1. 怎么跑

```text
cd backend
python -m pytest tests/acceptance/test_agent_full_link.py -v
# live 语音 + 真 LLM（需 backend/.env 的 ASR_*/TTS_*/LLM_*，PATH 有 ffmpeg）：
set AGENT_LIVE_VOICE=1
python -m pytest tests/acceptance/test_agent_full_link.py -v
```

conftest 默认 `ASR/TTS/CHAT_PROVIDER=stub`。本文件在 `AGENT_LIVE_VOICE=1` 时把 PROVIDER 改回 `volcengine` / `openai_compat`，凭证仍只读 `.env`。

本次实测：

| 条件 | 结果 |
|------|------|
| 无 live | **10 passed, 4 skipped**（约 2s；含新增 live-nav skip） |
| `AGENT_LIVE_VOICE=1`（修复后） | **13 passed**（约 57s） |
| `pytest backend/tests/` 无 live | **101 passed, 6 skipped** |

本地会写 `backend/tests/acceptance/_full_link_run.json`（UTF-8 摘要，**勿入库**）。

---

## 2. 模式一：自主介绍（tour → 镜头 → 讲解 → 可选 TTS）

### 2.1 路线规划

| 检查 | 结果 | 数据 |
|------|------|------|
| `POST /api/agent/tour` | ✅ | 200，`steps` 长度 **10** |
| 顺序 = `scene_graph.tour_path` | ✅ | 客厅 → 厨房 → 洗衣间 → 书房 → 卧室3 → 次卧 → 卫生间 → 主卧 → 卫生间3 → 卫生间2 |
| `index` 0 起递增 | ✅ | 0…9 |
| 每步 `room_id` / `trajectory_point_id` / `narration` 非空 | ✅ | 见下表 |
| 无 selling_points 则 omit | ✅ | 0330 房间无该字段，步骤中均未出现 |

### 2.2 镜头移动链（tp 白名单 + 点云坐标）

| 检查 | 结果 |
|------|------|
| 每步 tp ∈ `camera_poses.json` **85 键** | ✅ |
| `GET /api/camera_poses/w_0330_840483` 返回 85 条 | ✅ |
| 坐标 3 元、非 NaN、XY∈[-20,20]、Z∈[-0.5,4] | ✅ |
| 房间级 tp 的 Z（点云 Z-up 高度） | 全部 **0.5**（量级符合 z∈[0,3]） |

Tour 全链路清单：

| # | 房间 | tp_id | 点云 xyz (Z-up) | 讲解词 |
|---|------|-------|-----------------|--------|
| 0 | 客厅 | `tp_living` | `[0.061, 0.934, 0.5]` | 客厅约52.6平。 |
| 1 | 厨房 | `tp_kitchen` | `[-3.822, 3.256, 0.5]` | 厨房约5.6平。 |
| 2 | 洗衣间 | `tp_laundry` | `[-1.76, 5.47, 0.5]` | 洗衣间约4.3平。 |
| 3 | 书房 | `tp_study` | `[1.749, 2.986, 0.5]` | 书房约6.8平。 |
| 4 | 卧室3 | `tp_bedroom_3` | `[4.135, 4.762, 0.5]` | 卧室3约8.2平。 |
| 5 | 次卧 | `tp_bedroom_second` | `[-4.505, -4.146, 0.5]` | 次卧约12.5平。 |
| 6 | 卫生间 | `tp_bathroom` | `[-3.979, -0.66, 0.5]` | 卫生间约3平。 |
| 7 | 主卧 | `tp_bedroom_master` | `[4.415, -3.382, 0.5]` | 主卧约20.1平。 |
| 8 | 卫生间3 | `tp_bathroom_3` | `[5.439, 0.041, 0.5]` | 卫生间3约3.9平。 |
| 9 | 卫生间2 | `tp_bathroom_2` | `[4.87, 2.005, 0.5]` | 卫生间2约3.1平。 |

### 2.3 每房讲解 + 语音（live）

| 检查 | 结果 |
|------|------|
| `GET /api/agent/narration` × 10 | ✅ 均有 `reply_text`（与上表讲解词一致）；stub 下 omit `tts_url` |
| live TTS（客厅、主卧，`AGENT_LIVE_VOICE=1`） | ✅ `/static/tts/...mp3` 可 GET；客厅 21357 bytes / 1833ms；主卧 19437 bytes / 1839ms |

未对 10 房全部 TTS（可选；抽样 2 房已证明链路）。

---

## 3. 模式二：用户提问

规则版（stub LLM）始终跑；自由答与听想说在 live 下跑真 API。

| 用例 | 结果 | 关键数据 |
|------|------|----------|
| 导航「主卧在哪」 | ✅ | reply 含「主卧」；`teleport.tp_id=tp_bedroom_master`（白名单）；xyz `[4.415, -3.382, 0.5]`；无 `position` |
| 实例「沙发在哪」 | ✅ | reply「好的，带您去看沙发，在客厅。」；`tp_sofa_417`（teleport+highlight 各一条，同 tp） |
| 户型「这套房多大」 | ✅ | `120.1㎡，三室一厅。`；未编造待对拍 |
| 追问同 session：客厅 → 「那厨房呢」 | ✅ | 「好的，带您去厨房。厨房约5.6平。」；**无灶台** |
| 自由答「这套房适合什么人住」（live LLM） | ✅ | 非模板；见 §4；偶发降级规则版时测试会再打一枪 |
| 有灶台吗 | ✅ | 「没有关于「灶台」的可靠信息。」 |
| 钢琴在哪 | ✅ | 没有可靠信息；无 actions |
| 冰箱多大（0330） | ✅ | 「冰箱没有更多信息。」；**无 501 升** |
| 朝向怎么样 | ✅ | 「朝向数据未提供。」；未编造「南」 |
| `LLM_BASE_URL` 改错 | ✅ | HTTP 200，规则版兜底「带您去主卧…」；**不 500** |

---

## 4. AI 功能专项

### 4.1 路线规划

10 步动线与 GT `tour_path` 一致；每步房间 tp 均在 85 键白名单；无编造 tp。**可演示。**

### 4.2 镜头移动

`tp_id` → `GET /api/camera_poses` → 点云坐标链完整。房间锚点高度 Z=0.5m。前端用同一张表贴地即可。**可演示。**

### 4.3 语音交互（live，修复后）

| 段 | 结果 | 数据 |
|----|------|------|
| `TestClient` `POST /api/agent/asr` | ✅ | **「这栋房子的主卧在哪？」**；`duration_ms=3349`；**2215ms** |
| chat（转写 → 提问） | ✅ | **「好的，带您去主卧。主卧约20.1平。」** + `teleport.tp_id=tp_bedroom_master`；**2506ms** |
| TTS | ✅ | `/static/tts/6de3573e1ba14e37afdbabd35c6e7c2e.mp3`，30957 bytes；二次 POST **2ms**（chat 内已合成、命中缓存） |
| 闭环合计 | ✅ | ASR+chat+TTS ≈ **4.7s** |

导航 live（真 LLM 打开）：reply 仍是规则版「带您去主卧」，与 teleport 一致。

Live 自由答原文（LLM）：

> 从户型看，这套是三室一厅、面积约120.1㎡，比较适合需要三间独立房间的家庭，比如三口之家、与长辈同住，或需要书房/客房的住户。朝向、楼层、价格等信息未提供，无法判断更多适配情况。

非面积模板；grounded 户型/面积；未编造灶台/501/朝向。

---

## 5. 问题清单

| 级 | 问题 | 影响 | 状态 |
|----|------|------|------|
| 🔴 | ASR `async def` + `asyncio.run` → HTTP 空转写 | PTT 不通 | **已修**：`POST /asr` 改为同步 `def asr` + `UploadFile`，FastAPI 线程池执行 |
| 🟡 | LLM 把「主卧在哪」改成「无法判断在哪」却仍 teleport | 话术与镜头打架 | **已修（方案 B）**：`Intent.NAVIGATION` 不走 `enhance` |
| 🟡 | 讲解词 = 短 `story_card`，0330 无 selling_points | 带看偏报面积 | 数据侧补卖点（非本轮） |
| 🔵 | 沙发 teleport+highlight 同 `tp_sofa_417` | 低 | 前端去重或后端合一条 |
| 🔵 | live TTS 抽样 2 房 | 无 | 需要时可加 |
| 🔵 | 方舟自由答偶发失败 → 规则版面积句 | live 测试已加一次重试 | 依赖供应商 |
| 🔵 | Starlette TestClient 提示 httpx→httpx2 | 无 | 依赖升级 |

---

## 6. 防幻觉抽查

| 问 | 期望 | 实测 |
|----|------|------|
| 有灶台吗 | 不编造 U 型/配置 | ✅ 没有可靠信息 |
| 钢琴在哪 | 不编造 | ✅ 没有可靠信息 |
| 冰箱多大 | 不编造 501 升 | ✅ 没有更多信息 |
| 朝向 | 待对拍 → 数据未提供 | ✅ 未出现「南」 |
| 面积 | 只用 120.1 | ✅ |
| 厨房追问 | 5.6㎡、无灶台 | ✅ |

---

## 7. 结论（能否进演示）

| 能力 | 建议 |
|------|------|
| 自主介绍（tour 10 步 + tp→坐标 + 每房讲解） | **可以进演示** |
| 用户提问（导航/实例/户型/追问/防幻觉/LLM 失败兜底） | **可以进演示** |
| 自由答（真 LLM） | **可以进演示**（话术已 grounded） |
| 听想说 PTT（前端 → `POST /asr`） | **可以进演示**（HTTP 已出真实转写；闭环约 4.7s） |
| 讲解配音 | live TTS 已出 mp3；可给 tour/narration 按需合成 |

**总评**：双模式 + 镜头链 + 听想说 HTTP 已闭环，可进演示。导航用规则版「带您去{房间}」，自由问答仍走 LLM。讲解文案偏短（story_card）不挡主路径。

不要 add `_full_link_run.json` / `.env`。

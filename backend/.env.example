# Copy to .env locally. Do not put real keys here.
CORS_ORIGINS=http://localhost:5173
# AGENT_BASE_URL=http://127.0.0.1:8001
# 理解层 Provider：gt（默认，读 mock scene_graph）| dual_engine（未实现）
UNDERSTANDING_PROVIDER=gt

# ---- agent 真实 API（key 只写 .env，本文件留空）----
# Provider：stub（默认，demo 不依赖外网）| openai_compat
ASR_PROVIDER=stub
TTS_PROVIDER=stub
CHAT_PROVIDER=stub

# 待确认：向 key 提供方确认 base_url（是否含 /v1）、模型名、ASR/TTS 端点路径
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
ASR_API_KEY=
ASR_BASE_URL=
ASR_MODEL=
TTS_API_KEY=
TTS_BASE_URL=
TTS_MODEL=

